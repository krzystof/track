#!/usr/bin/env ruby
$LOAD_PATH.unshift File.dirname(File.realpath(__FILE__)) + "/../lib"

require 'cli'
require 'console_input'
require 'console_output'

Cli.run(ConsoleInput.new(ARGV), ConsoleOutput.new)
